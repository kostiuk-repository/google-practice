package topic01_arrays_strings.practice;

public class Medium_02_02_SpiralMatrix {

    /**
     * Drill 2/5 for Medium_02_SpiralMatrix — Generate Spiral Matrix
     * Technique: the same 4 shrinking boundary pointers (top, bottom, left,
     * right) as the original problem, but run in the WRITE direction instead
     * of the read direction — filling in increasing values 1, 2, 3, ... as the
     * pointers walk the clockwise spiral, instead of collecting values that
     * are already there.
     *
     * Given an integer n, generate an n x n matrix filled with the values
     * 1 to n*n in clockwise spiral order, and return it.
     *
     * Example:
     * Input: n = 3
     * Output: [[1,2,3],
     *          [8,9,4],
     *          [7,6,5]]
     */
    public int[][] generateSpiralMatrix(int n) {
        // TODO: Implement your solution here.
        return new int[0][0];
    }
}
