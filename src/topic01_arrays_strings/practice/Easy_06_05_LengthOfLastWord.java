package topic01_arrays_strings.practice;

public class Easy_06_05_LengthOfLastWord {

    /**
     * Drill 5/5 for Easy_06_LengthOfLastWord — Longest Word Length
     * Technique: manual character-by-character scan skipping delimiter runs
     * and counting each word's length, extended with a running-max aggregate
     * that keeps the longest word length seen so far across the whole scan.
     *
     * Given a string s consisting of words and spaces, return the length of
     * the LONGEST word in the string. A word is a maximal substring
     * consisting of non-space characters only. Do not use split() or trim().
     *
     * Example:
     * Input: s = "   fly me   to   the moon  "
     * Output: 4
     * Explanation: Word lengths are fly=3, me=2, to=2, the=3, moon=4. The
     * longest is "moon" with length 4.
     */
    public int longestWordLength(String s) {
        // TODO: Implement your solution here.
        return 0;
    }
}
