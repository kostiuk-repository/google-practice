package topic01_arrays_strings.practice;

public class Easy_07_03_MonotonicArray {

    /**
     * Drill 3/5 for Easy_07_MonotonicArray — Decreasing Only
     * Technique: single pass with a single boolean flag that gets set to false on a
     * violation — the mirror image of Drill 2, keeping only the "isDecreasing" flag from
     * Easy_07_MonotonicArray's two-flag approach.
     *
     * Given an integer array nums, return true if the array is monotone decreasing
     * (nums[i] >= nums[i+1] for all consecutive pairs), or false otherwise. Increasing
     * arrays should return false here, unlike the general monotonic check.
     *
     * Example:
     * Input: nums = [5,4,4,2]
     * Output: true
     *
     * Example 2:
     * Input: nums = [1,2,3]
     * Output: false
     */
    public boolean isMonotonicDecreasing(int[] nums) {
        // TODO: Implement your solution here.
        return false;
    }
}
