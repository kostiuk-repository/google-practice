package topic01_arrays_strings.practice;

public class Easy_08_03_LargestNumberTwice {

    /**
     * Drill 3/5 for Easy_08_LargestNumberTwice — Max Minus Second Max
     * Technique: single pass tracking both largest and second-largest at once, exactly like
     * Easy_08_LargestNumberTwice's internal helper trackers, but now the answer is derived
     * from combining both tracked values instead of comparing against a threshold.
     *
     * Given an integer array nums with at least two elements, track the largest and
     * second-largest values in a single pass and return the difference (largest -
     * secondLargest). Duplicate values are allowed; if the largest value appears more than
     * once, the second-largest equals the largest.
     *
     * Example:
     * Input: nums = [10,4,7,2]
     * Output: 3
     * Explanation: largest = 10, secondLargest = 7, difference = 3.
     *
     * Example 2:
     * Input: nums = [5,5,3]
     * Output: 0
     * Explanation: largest = 5, secondLargest = 5 (the duplicate), difference = 0.
     */
    public int maxMinusSecondMax(int[] nums) {
        // TODO: Implement your solution here.
        return 0;
    }
}
