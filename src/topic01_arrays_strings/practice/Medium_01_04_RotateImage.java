package topic01_arrays_strings.practice;

public class Medium_01_04_RotateImage {

    /**
     * Drill 4/5 for Medium_01_RotateImage — Has Rotational Symmetry
     * Technique: apply the same rotate-90-clockwise logic (transpose + reverse
     * rows) from the original problem, but on a scratch copy, then compare the
     * rotated copy against the original instead of returning it. Reuses the
     * exact transformation as a building block for a yes/no check.
     *
     * You are given an n x n 2D matrix. Return true if rotating the matrix 90
     * degrees clockwise produces the exact same matrix (i.e. the matrix has
     * 90-degree rotational symmetry), and false otherwise. The input matrix
     * must be left unmodified.
     *
     * Example:
     * Input: matrix = [[1,2,1],
     *                  [2,3,2],
     *                  [1,2,1]]
     * Output: true
     *
     * Example 2:
     * Input: matrix = [[1,2,3],
     *                  [4,5,6],
     *                  [7,8,9]]
     * Output: false
     */
    public boolean hasRotationalSymmetry(int[][] matrix) {
        // TODO: Implement your solution here.
        return false;
    }
}
