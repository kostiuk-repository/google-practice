package topic01_arrays_strings.practice;

public class Easy_06_01_LengthOfLastWord {

    /**
     * Drill 1/5 for Easy_06_LengthOfLastWord — Length Of First Word
     * Technique: manual character-by-character scan, but from the FRONT of
     * the string instead of the end — skip leading spaces, then count
     * characters until the next space or the end of the string. Same loop
     * shape as Easy_06_LengthOfLastWord, opposite direction.
     *
     * Given a string s consisting of words and spaces, return the length of
     * the FIRST word in the string. A word is a maximal substring consisting
     * of non-space characters only. Do not use split() or trim().
     *
     * Example:
     * Input: s = "   fly me   to   the moon  "
     * Output: 3
     * Explanation: The first word is "fly" with length 3.
     */
    public int lengthOfFirstWord(String s) {
        // TODO: Implement your solution here.
        return 0;
    }
}
