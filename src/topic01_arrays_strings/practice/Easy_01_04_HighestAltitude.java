package topic01_arrays_strings.practice;

public class Easy_01_04_HighestAltitude {

    /**
     * Drill 4/5 for Easy_01_HighestAltitude — Altitude Range
     * Technique: running accumulator, tracking TWO best-so-far values at once
     * (max so far AND min so far in the same single pass).
     *
     * Same road trip setup as Easy_01_HighestAltitude: the biker starts at altitude 0,
     * and gain[i] is the net change in altitude between point i and i + 1.
     * Return the range of the trip: the highest altitude reached minus the lowest
     * altitude reached (including the starting altitude 0).
     *
     * Example:
     * Input: gain = [-5,1,5,0,-7]
     * Output: 7
     * Explanation: The altitudes are [0,-5,-4,1,1,-6]. Highest = 1, lowest = -6, range = 7.
     */
    public int altitudeRange(int[] gain) {
        // TODO: Implement your solution here.
        return 0;
    }
}
