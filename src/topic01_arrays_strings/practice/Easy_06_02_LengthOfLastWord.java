package topic01_arrays_strings.practice;

public class Easy_06_02_LengthOfLastWord {

    /**
     * Drill 2/5 for Easy_06_LengthOfLastWord — Count Words
     * Technique: manual character-by-character scan skipping delimiter runs,
     * but instead of measuring one token, count the number of transitions
     * from a space-run into a non-space-run across the whole string.
     *
     * Given a string s consisting of words and spaces, return the number of
     * words in the string. A word is a maximal substring consisting of
     * non-space characters only. Do not use split() or trim().
     *
     * Example:
     * Input: s = "   fly me   to   the moon  "
     * Output: 5
     */
    public int countWords(String s) {
        // TODO: Implement your solution here.
        return 0;
    }
}
