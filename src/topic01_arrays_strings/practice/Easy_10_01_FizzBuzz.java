package topic01_arrays_strings.practice;

import java.util.ArrayList;
import java.util.List;

public class Easy_10_01_FizzBuzz {

    /**
     * Drill 1/5 for Easy_10_FizzBuzz — FizzBuzz Codes
     * Technique: iterate 1..n and build output per index using the same divisibility checks
     * as Easy_10_FizzBuzz, but emit an integer code instead of a string, isolating the
     * "classify each number" logic from the "render as text" logic.
     *
     * Given an integer n, return a List<Integer> (1-indexed conceptually, list has n entries)
     * where each entry is: 0 for a plain number (not divisible by 3 or 5), 1 for "Fizz"
     * (divisible by 3 only), 2 for "Buzz" (divisible by 5 only), 3 for "FizzBuzz" (divisible
     * by both 3 and 5).
     *
     * Example:
     * Input: n = 5
     * Output: [0,0,1,0,2]
     * Explanation: 1->0, 2->0, 3->Fizz(1), 4->0, 5->Buzz(2).
     */
    public List<Integer> fizzBuzzCodes(int n) {
        // TODO: Implement your solution here.
        return null;
    }
}
