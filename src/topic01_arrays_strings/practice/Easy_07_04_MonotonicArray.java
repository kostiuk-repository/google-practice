package topic01_arrays_strings.practice;

public class Easy_07_04_MonotonicArray {

    /**
     * Drill 4/5 for Easy_07_MonotonicArray — Longest Monotonic Prefix
     * Technique: extend the early-exit boolean-flag idea into a counter — instead of just
     * returning false the moment a violation is found (as in Easy_07_MonotonicArray), stop
     * the pass at that point and report how far you got.
     *
     * Given an integer array nums, return the length of the longest prefix of nums (starting
     * at index 0) that is monotonic (either non-decreasing or non-increasing the whole way).
     * A single element counts as a monotonic prefix of length 1.
     *
     * Example:
     * Input: nums = [1,2,3,2,5]
     * Output: 3
     * Explanation: [1,2,3] is monotone increasing; adding nums[3] = 2 breaks it.
     */
    public int longestMonotonicPrefix(int[] nums) {
        // TODO: Implement your solution here.
        return 0;
    }
}
