package topic01_arrays_strings.practice;

public class Easy_06_03_LengthOfLastWord {

    /**
     * Drill 3/5 for Easy_06_LengthOfLastWord — Last Word Text
     * Technique: same manual scan from the end, skipping trailing spaces and
     * then walking back over the last word — but instead of just returning a
     * count, return the actual substring collected during the scan.
     *
     * Given a string s consisting of words and spaces, return the last word
     * itself as a String. A word is a maximal substring consisting of
     * non-space characters only. Do not use split() or trim().
     *
     * Example:
     * Input: s = "Hello World  "
     * Output: "World"
     */
    public String lastWordText(String s) {
        // TODO: Implement your solution here.
        return "";
    }
}
