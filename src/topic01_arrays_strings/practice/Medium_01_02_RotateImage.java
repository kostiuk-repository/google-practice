package topic01_arrays_strings.practice;

public class Medium_01_02_RotateImage {

    /**
     * Drill 2/5 for Medium_01_RotateImage — Rotate 180 Degrees
     * Technique: in-place matrix transformation via direct opposite-corner swaps
     * (mat[i][j] swapped with mat[n-1-i][n-1-j] for every cell) — equivalent to
     * applying the 90-degree clockwise rotation twice, but done directly in one
     * pass instead of two full transpose+reverse passes.
     *
     * You are given an n x n 2D matrix representing an image. Rotate the image
     * by 180 degrees, in place. Do not allocate another 2D matrix to do the
     * rotation.
     *
     * Example:
     * Input: matrix = [[1,2,3],
     *                  [4,5,6],
     *                  [7,8,9]]
     * Output: [[9,8,7],
     *          [6,5,4],
     *          [3,2,1]]
     */
    public void rotate180(int[][] matrix) {
        // TODO: Implement your solution here in-place.
    }
}
