package topic01_arrays_strings.practice;

public class Easy_07_02_MonotonicArray {

    /**
     * Drill 2/5 for Easy_07_MonotonicArray — Increasing Only
     * Technique: single pass with a single boolean flag that gets set to false on a
     * violation — a narrowed-down version of Easy_07_MonotonicArray's two-flag approach,
     * keeping only the "isIncreasing" flag.
     *
     * Given an integer array nums, return true if the array is monotone increasing
     * (nums[i] <= nums[i+1] for all consecutive pairs), or false otherwise. Decreasing
     * arrays should return false here, unlike the general monotonic check.
     *
     * Example:
     * Input: nums = [1,1,2,3]
     * Output: true
     *
     * Example 2:
     * Input: nums = [3,2,1]
     * Output: false
     */
    public boolean isMonotonicIncreasing(int[] nums) {
        // TODO: Implement your solution here.
        return false;
    }
}
