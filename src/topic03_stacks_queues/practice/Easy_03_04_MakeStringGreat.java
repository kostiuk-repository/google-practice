package topic03_stacks_queues.practice;

public class Easy_03_04_MakeStringGreat {

    /**
     * Drill 4/5 for Easy_03_MakeStringGreat — Count Removed Pairs
     * Technique: same stack-based cancellation loop as Easy_03_MakeStringGreat, but instead
     * of returning the resulting string, track and return how many adjacent bad pairs were
     * removed in total (a different aggregation of the same simulation).
     *
     * Given a string s of lower and upper case English letters, repeatedly remove adjacent
     * characters that are the same letter in different cases, until the string is good.
     * Return the total number of removal operations performed (each operation removes one
     * pair, i.e. two characters).
     *
     * Example:
     * Input: s = "leEeetcode"
     * Output: 1
     * Explanation: Only "eE" is removed, which is a single removal operation.
     */
    public int countRemovedPairs(String s) {
        // TODO: Implement your solution here.
        return 0;
    }
}
