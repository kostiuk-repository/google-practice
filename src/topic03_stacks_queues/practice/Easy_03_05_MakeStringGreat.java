package topic03_stacks_queues.practice;

public class Easy_03_05_MakeStringGreat {

    /**
     * Drill 5/5 for Easy_03_MakeStringGreat — Make Array Great
     * Technique: capstone that generalizes the adjacent-cancellation stack from the string
     * domain to a numeric array domain — instead of "same letter, different case", two
     * adjacent numbers cancel when they are exact opposites (x and -x).
     *
     * Given an integer array nums, repeatedly remove any two adjacent elements x and -x
     * (in either order), until no such adjacent pair remains. Return the final array.
     *
     * Example:
     * Input: nums = [6,-3,3,4]
     * Output: [6,4]
     * Explanation: [-3,3] is a matching pair and is removed, leaving [6,4], which has no
     * more adjacent opposite pairs.
     */
    public int[] makeArrayGreat(int[] nums) {
        // TODO: Implement your solution here.
        return new int[0];
    }
}
