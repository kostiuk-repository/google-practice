package topic03_stacks_queues.practice;

public class Easy_03_01_MakeStringGreat {

    /**
     * Drill 1/5 for Easy_03_MakeStringGreat — Is Bad Pair
     * Technique: isolate the cancellation predicate used by Easy_03_MakeStringGreat — two
     * characters cancel when they are the same letter but different case, which can be
     * checked purely with ASCII arithmetic (the distance between 'a' and 'A' is 32) instead
     * of calling Character.toLowerCase().
     *
     * Given two characters a and b, return true if they represent the same English letter
     * but with different case (e.g. 'e' and 'E'), and false otherwise. Do not use
     * Character.toLowerCase() or Character.toUpperCase().
     *
     * Example:
     * Input: a = 'e', b = 'E'
     * Output: true
     *
     * Example 2:
     * Input: a = 'a', b = 'b'
     * Output: false
     */
    public boolean isBadPair(char a, char b) {
        // TODO: Implement your solution here.
        return false;
    }
}
