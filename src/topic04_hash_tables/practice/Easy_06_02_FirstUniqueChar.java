package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_06_02_FirstUniqueChar {

    /**
     * Drill 2/5 for Easy_06_FirstUniqueChar — Last Unique Character Index
     * Technique: same frequency-map-then-scan technique as Easy_06_FirstUniqueChar, but the
     * second pass scans in the OPPOSITE direction (right to left) to find the last qualifying
     * character instead of the first.
     *
     * Given a string s, find the LAST non-repeating character in it (scanning from the end
     * toward the start) and return its index in the original string. If it does not exist,
     * return -1.
     *
     * Example:
     * Input: s = "leetcode"
     * Output: 6
     * Explanation: the non-repeating characters are 'l'(0), 't'(3), 'c'(4), 'o'(5), 'd'(6);
     * 'd' at index 6 is the last one when scanning from the end.
     */
    public int lastUniqCharIndex(String s) {
        // TODO: Implement your solution here.
        return -1;
    }
}
