package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_06_04_FirstUniqueChar {

    /**
     * Drill 4/5 for Easy_06_FirstUniqueChar — First Unique Word In a Sentence
     * Technique: generalizes the frequency-map granularity from single CHARACTERS in
     * Easy_06_FirstUniqueChar to whole WORDS — same two-pass "count, then scan for count == 1"
     * shape, just tokenized differently.
     *
     * Given a sentence s of space-separated words, find the first word that does not repeat
     * anywhere else in the sentence and return it. If every word repeats, return an empty
     * string.
     *
     * Example:
     * Input: s = "the cat sat on the mat the cat ran"
     * Output: "sat"
     * Explanation: "the" and "cat" both repeat; "sat" is the first word that appears only once.
     */
    public String firstUniqueWord(String s) {
        // TODO: Implement your solution here.
        return null;
    }
}
