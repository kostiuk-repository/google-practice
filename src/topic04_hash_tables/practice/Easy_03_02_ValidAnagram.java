package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_03_02_ValidAnagram {

    /**
     * Drill 2/5 for Easy_03_ValidAnagram — K-Anagram Check
     * Technique: builds the same frequency-difference comparison as Easy_03_ValidAnagram, but
     * instead of requiring the difference to be exactly zero, it tolerates up to k mismatched
     * character replacements.
     *
     * Given two strings s and t of equal length and an integer k, return true if t can be
     * turned into an anagram of s by changing at most k characters of t.
     *
     * Example:
     * Input: s = "anagram", t = "grammar", k = 2
     * Output: true
     */
    public boolean isKAnagram(String s, String t, int k) {
        // TODO: Implement your solution here.
        return false;
    }
}
