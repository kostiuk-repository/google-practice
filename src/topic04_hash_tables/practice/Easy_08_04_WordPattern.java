package topic04_hash_tables.practice;

import java.util.List;

public class Easy_08_04_WordPattern {

    /**
     * Drill 4/5 for Easy_08_WordPattern — Multi-Token Pattern (Pattern of Words, Not Chars)
     * Technique: generalizes the "unit" on the pattern side from a single CHARACTER (as in
     * Easy_08_WordPattern) to an arbitrary STRING token — the same two-map bijection check
     * still applies, just keyed by String -> String instead of Character -> String.
     *
     * Given a list of pattern tokens patternTokens and a list of words words (same length),
     * return true if there is a bijection between patternTokens and words: every occurrence of
     * a pattern token maps to the same word, and no two distinct pattern tokens map to the
     * same word.
     *
     * Example:
     * Input: patternTokens = ["X","Y","Y","X"], words = ["dog","cat","cat","dog"]
     * Output: true
     */
    public boolean followsTokenPattern(List<String> patternTokens, List<String> words) {
        // TODO: Implement your solution here.
        return false;
    }
}
