package topic04_hash_tables.practice;

public class Easy_07_04_IsomorphicStrings {

    /**
     * Drill 4/5 for Easy_07_IsomorphicStrings — Isomorphic Integer Arrays
     * Technique: generalizes the token type of the two-way bijection check in
     * Easy_07_IsomorphicStrings from characters to arbitrary integers — the same "two HashMaps,
     * one per direction" logic, just keyed by Integer instead of Character.
     *
     * Given two integer arrays a and b of equal length, return true if there is a bijection
     * between the values of a and the values of b such that replacing every value in a with
     * its mapped value produces exactly b (and no two distinct values in a map to the same
     * value in b).
     *
     * Example:
     * Input: a = [1,2,2,1], b = [5,7,7,5]
     * Output: true
     */
    public boolean isIsomorphicArray(int[] a, int[] b) {
        // TODO: Implement your solution here.
        return false;
    }
}
