package topic04_hash_tables.practice;

import java.util.List;

public class Easy_07_03_IsomorphicStrings {

    /**
     * Drill 3/5 for Easy_07_IsomorphicStrings — Count Isomorphic Matches In a List
     * Technique: applies the same two-map bijection check from Easy_07_IsomorphicStrings
     * repeatedly, once per candidate, turning a single pairwise check into an aggregate count.
     *
     * Given a reference string s and a list of candidate strings candidates, return the number
     * of strings in candidates that are isomorphic to s.
     *
     * Example:
     * Input: s = "egg", candidates = ["add", "foo", "abb", "aaa"]
     * Output: 2
     * Explanation: "add" is isomorphic to "egg" (e->a, g->d). "abb" is isomorphic to "egg"
     * too (e->a, g->b). "foo" and "aaa" are not.
     */
    public int countIsomorphicMatches(String s, List<String> candidates) {
        // TODO: Implement your solution here.
        return 0;
    }
}
