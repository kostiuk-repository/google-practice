package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_03_01_ValidAnagram {

    /**
     * Drill 1/5 for Easy_03_ValidAnagram — Character Frequency Map
     * Technique: isolates the counting sub-step that powers Easy_03_ValidAnagram — build the
     * character frequency table itself instead of comparing two strings with it.
     *
     * Given a string s, return a Map<Character, Integer> where each key is a character that
     * appears in s and its value is how many times that character occurs.
     *
     * Example:
     * Input: s = "aabbc"
     * Output: {a=2, b=2, c=1}
     */
    public Map<Character, Integer> characterFrequency(String s) {
        // TODO: Implement your solution here.
        return new HashMap<>();
    }
}
