package topic04_hash_tables.practice;

import java.util.List;

public class Easy_03_03_ValidAnagram {

    /**
     * Drill 3/5 for Easy_03_ValidAnagram — All Words Are Anagrams Of Each Other
     * Technique: generalizes the pairwise frequency-map comparison from Easy_03_ValidAnagram
     * from two strings to a whole list — every word's frequency signature must match the
     * first word's.
     *
     * Given a list of strings words, return true if every string in the list is an anagram of
     * every other string in the list (they all use the exact same multiset of characters).
     *
     * Example:
     * Input: words = ["listen", "silent", "enlist"]
     * Output: true
     */
    public boolean allMutualAnagrams(List<String> words) {
        // TODO: Implement your solution here.
        return false;
    }
}
