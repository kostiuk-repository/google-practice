package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_04_01_IntersectionTwoArrays {

    /**
     * Drill 1/5 for Easy_04_IntersectionTwoArrays — Intersection With Multiplicities
     * Technique: extends the HashSet membership check from Easy_04_IntersectionTwoArrays into
     * a frequency HashMap, so each shared value appears in the result as many times as it is
     * shared (min of its counts in both arrays), not just once.
     *
     * Given two integer arrays nums1 and nums2, return an array of their intersection where
     * each element appears as many times as it shows up in BOTH arrays (the minimum of its
     * counts in nums1 and nums2). You may return the result in any order.
     *
     * Example:
     * Input: nums1 = [1,2,2,1], nums2 = [2,2]
     * Output: [2,2]
     */
    public int[] intersectionWithCounts(int[] nums1, int[] nums2) {
        // TODO: Implement your solution here.
        return new int[0];
    }
}
