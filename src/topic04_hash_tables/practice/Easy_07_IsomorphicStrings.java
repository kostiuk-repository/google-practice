package topic04_hash_tables.practice;

import java.util.HashMap;
import java.util.Map;

public class Easy_07_IsomorphicStrings {

    /**
     * Problem: Isomorphic Strings
     * 
     * Two strings s and t are isomorphic if the characters in s can be replaced to get t.
     * All occurrences of a character must be replaced with another character while preserving 
     * the order of characters. No two characters may map to the same character, 
     * but a character may map to itself.
     * 
     * Example:
     * Input: s = "egg", t = "add"
     * Output: true
     * 
     * Example 2:
     * Input: s = "foo", t = "bar"
     * Output: false
     */
    public boolean isIsomorphic(String s, String t) {
        // TODO: Implement your solution here.
        return false;
    }

    /* 
     * =========================================================================
     * MENTEE QUESTION REGION
     * =========================================================================
     * Your Question: 
     * 
     */

    /* 
     * =========================================================================
     * MENTOR QUESTION TO MENTEE
     * =========================================================================
     * Mentor Question: Why do we need two maps (or a map and a set) to solve this problem? 
     * Give an example of s and t that would return a false positive (i.e. returning true 
     * when it is actually false) if we only map s characters to t characters, but don't check 
     * the reverse mapping.
     * 
     * Your Answer: 
     * 
     */
}
