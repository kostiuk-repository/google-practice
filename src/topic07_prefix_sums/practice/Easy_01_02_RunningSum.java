package topic07_prefix_sums.practice;

public class Easy_01_02_RunningSum {

    /**
     * Drill 2/5 for Easy_01_RunningSum — Running Sum With Starting Balance
     * Technique: same accumulator loop as Easy_01_RunningSum, but the accumulator is
     * seeded with a nonzero initial value instead of starting at 0.
     *
     * Given an array nums and an integer start, return an array result where
     * result[i] = start + nums[0] + nums[1] + ... + nums[i].
     *
     * Example:
     * Input: nums = [1,2,3], start = 100
     * Output: [101,103,106]
     */
    public int[] runningSumWithStart(int[] nums, int start) {
        // TODO: Implement your solution here.
        return new int[0];
    }
}
