package topic07_prefix_sums.practice;

public class Easy_03_01_HighestAltitude {

    /**
     * Drill 1/5 for Easy_03_HighestAltitude — Altitude Profile
     * Technique: isolate the BUILD step of Easy_03_HighestAltitude — instead of tracking
     * only the maximum altitude, build and return the full array of altitudes at every point.
     *
     * A biker starts at altitude 0. You are given an array gain where gain[i] is the net
     * altitude change between point i and point i + 1. Return an array altitudes of length
     * gain.length + 1, where altitudes[i] is the altitude at point i (altitudes[0] = 0).
     *
     * Example:
     * Input: gain = [-5,1,5,0,-7]
     * Output: [0,-5,-4,1,1,-6]
     */
    public int[] altitudeProfile(int[] gain) {
        // TODO: Implement your solution here.
        return new int[0];
    }
}
