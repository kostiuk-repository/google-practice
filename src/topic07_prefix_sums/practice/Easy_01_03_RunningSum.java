package topic07_prefix_sums.practice;

public class Easy_01_03_RunningSum {

    /**
     * Drill 3/5 for Easy_01_RunningSum — Running Product
     * Technique: swap the accumulator's operator from + to * — the same "carry a
     * running total forward" shape as Easy_01_RunningSum, generalized to multiplication.
     *
     * Given an array nums of nonzero integers, return an array result where
     * result[i] = nums[0] * nums[1] * ... * nums[i] (the running product up to i).
     *
     * Example:
     * Input: nums = [1,2,3,4]
     * Output: [1,2,6,24]
     */
    public int[] runningProduct(int[] nums) {
        // TODO: Implement your solution here.
        return new int[0];
    }
}
