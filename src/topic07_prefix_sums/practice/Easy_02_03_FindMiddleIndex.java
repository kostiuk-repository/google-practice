package topic07_prefix_sums.practice;

public class Easy_02_03_FindMiddleIndex {

    /**
     * Drill 3/5 for Easy_02_FindMiddleIndex — Ratio Middle Index
     * Technique: generalize the equality condition leftSum == rightSum from
     * Easy_02_FindMiddleIndex into a weighted comparison leftSum == ratio * rightSum.
     *
     * Given an array nums and an integer ratio, return the first index i such that the
     * sum of elements to its left equals ratio times the sum of elements to its right.
     * Return -1 if no such index exists.
     *
     * Example:
     * Input: nums = [8,2,0,2], ratio = 4
     * Output: 1
     * Explanation: At i=1: leftSum = 8, rightSum = 0+2 = 2, and 8 == 4 * 2, so index 1 works.
     */
    public int findRatioMiddleIndex(int[] nums, int ratio) {
        // TODO: Implement your solution here.
        return -1;
    }
}
