package topic06_sliding_window.practice;

public class Easy_05_05_MaxVowels {

    /**
     * Drill 5/5 for Easy_05_MaxVowels — Fewest-Vowel Window Text
     * Technique: capstone — mirrors the comparison direction of Easy_05_MaxVowels (minimum instead
     * of maximum) AND changes the return type from a count to the actual window TEXT, combining
     * both variations on top of the same running-counter window.
     *
     * Given a string s and an integer k, find the leftmost substring of length k that contains the
     * FEWEST vowels, and return that substring itself (not its vowel count).
     *
     * Example:
     * Input: s = "abciiidef", k = 3
     * Output: "abc"
     * Explanation: "abc" has 1 vowel, tied for fewest with "bci" (1) and "def" (1), but "abc" is
     * the leftmost of the tied windows.
     */
    public String minVowelSubstring(String s, int k) {
        // TODO: Implement your solution here.
        return "";
    }
}
