package topic06_sliding_window.practice;

public class Easy_04_03_KBeautyOfNumber {

    /**
     * Drill 3/5 for Easy_04_KBeautyOfNumber — Sum of Divisor Windows
     * Technique: same fixed-size window plus divisor check as Easy_04_KBeautyOfNumber, but the
     * aggregation changes from COUNTING qualifying windows to SUMMING their values.
     *
     * Given an integer num and an integer k, convert num to its digit string. For every
     * contiguous substring of length k whose integer value is nonzero and evenly divides num,
     * add that value to a running total. Return the total.
     *
     * Example:
     * Input: num = 240, k = 2
     * Output: 64
     * Explanation: The digit string is "240". Windows "24" and "40" both evenly divide 240
     * (240 / 24 = 10, 240 / 40 = 6). Sum: 24 + 40 = 64.
     */
    public int sumOfDivisorSubstrings(int num, int k) {
        // TODO: Implement your solution here.
        return 0;
    }
}
