package topic06_sliding_window.practice;

public class Easy_05_03_MaxVowels {

    /**
     * Drill 3/5 for Easy_05_MaxVowels — Max Vowels Across a Range of Window Lengths
     * Technique: same running vowel-counter window as Easy_05_MaxVowels, but generalized so the
     * window length is no longer a single fixed k — instead we try every length from kMin to kMax
     * and keep the best result across all of them.
     *
     * Given a string s and two integers kMin and kMax, return the maximum number of vowels found
     * in any substring of s whose length is between kMin and kMax (inclusive).
     *
     * Example:
     * Input: s = "abciiidef", kMin = 2, kMax = 3
     * Output: 3
     * Explanation: The length-3 substring "iii" contains 3 vowels, the most achievable within
     * the allowed window lengths.
     */
    public int maxVowelsVariableLength(String s, int kMin, int kMax) {
        // TODO: Implement your solution here.
        return 0;
    }
}
