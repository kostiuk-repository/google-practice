package topic06_sliding_window.practice;

public class Easy_04_04_KBeautyOfNumber {

    /**
     * Drill 4/5 for Easy_04_KBeautyOfNumber — Largest Divisor Window
     * Technique: same fixed-size window plus divisor check as Easy_04_KBeautyOfNumber, but instead
     * of counting how many windows qualify we track the MAXIMUM qualifying value seen (argmax).
     *
     * Given an integer num and an integer k, convert num to its digit string. Among all contiguous
     * substrings of length k whose integer value is nonzero and evenly divides num, return the
     * largest such value. If none qualify, return -1.
     *
     * Example:
     * Input: num = 240, k = 2
     * Output: 40
     * Explanation: The digit string is "240". Windows "24" and "40" both evenly divide 240;
     * the larger of the two is 40.
     */
    public int largestDivisorWindow(int num, int k) {
        // TODO: Implement your solution here.
        return -1;
    }
}
