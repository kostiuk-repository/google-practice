package topic06_sliding_window.practice;

public class Easy_01_01_MaxAverageSubarray {

    /**
     * Drill 1/5 for Easy_01_MaxAverageSubarray — Max Sum Subarray of Size K
     * Technique: fixed-size sliding window, O(1) update per step (add the entering element,
     * subtract the leaving element) — the same window-sum core used by Easy_01_MaxAverageSubarray,
     * isolated here without the final division by k.
     *
     * Given an integer array nums and an integer k, find the maximum possible SUM (not average)
     * of any contiguous subarray of length k.
     *
     * Example:
     * Input: nums = [2,1,5,1,3,2], k = 3
     * Output: 9
     * Explanation: The subarray [5,1,3] has the maximum sum, 9.
     */
    public int maxSumSubarray(int[] nums, int k) {
        // TODO: Implement your solution here.
        return 0;
    }
}
