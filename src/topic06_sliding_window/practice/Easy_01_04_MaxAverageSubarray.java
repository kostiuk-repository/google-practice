package topic06_sliding_window.practice;

public class Easy_01_04_MaxAverageSubarray {

    /**
     * Drill 4/5 for Easy_01_MaxAverageSubarray — Index of Max Average Window
     * Technique: same fixed-size window sum update as Easy_01_MaxAverageSubarray, but the
     * accumulator now also tracks the STARTING INDEX of the best window seen so far, not just
     * the best value itself.
     *
     * Given an integer array nums and an integer k, return the starting index of the contiguous
     * subarray of length k with the maximum average. If multiple windows tie for the maximum,
     * return the smallest starting index.
     *
     * Example:
     * Input: nums = [1,12,-5,-6,50,3], k = 4
     * Output: 1
     * Explanation: The subarray starting at index 1, [12,-5,-6,50], has average 12.75, the highest.
     */
    public int indexOfMaxAverageWindow(int[] nums, int k) {
        // TODO: Implement your solution here.
        return 0;
    }
}
