package topic06_sliding_window.practice;

public class Easy_05_04_MaxVowels {

    /**
     * Drill 4/5 for Easy_05_MaxVowels — Index of the Max-Vowel Window
     * Technique: same fixed-size window with running vowel counter as Easy_05_MaxVowels, but
     * instead of returning just the best count we track the STARTING INDEX where that best count
     * first occurs (argmax location).
     *
     * Given a string s and an integer k, return the starting index of the leftmost substring of
     * length k that contains the maximum possible number of vowels.
     *
     * Example:
     * Input: s = "abciiidef", k = 3
     * Output: 3
     * Explanation: The substring "iii" starting at index 3 has 3 vowels, more than any other
     * length-3 window.
     */
    public int indexOfMaxVowelWindow(String s, int k) {
        // TODO: Implement your solution here.
        return 0;
    }
}
