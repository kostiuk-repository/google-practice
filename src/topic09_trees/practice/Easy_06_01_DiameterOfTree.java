package topic09_trees.practice;

import topic09_trees.TreeNode;

public class Easy_06_01_DiameterOfTree {

    /**
     * Drill 1/5 for Easy_06_DiameterOfTree — Height Of Tree
     * Technique: Easy_06_DiameterOfTree computes, for every node, the height of its left
     * and right subtrees via post-order recursion and combines them into a diameter. This
     * drill isolates just the height sub-step: return 1 + max(leftHeight, rightHeight).
     *
     * Given the root of a binary tree, return its height (the number of edges on the
     * longest path from the root down to a leaf). An empty tree has height -1, and a
     * single-node tree has height 0.
     *
     * Example:
     * Input: root = [1,2,3,4,5]
     * Output: 2
     * Explanation: the longest root-to-leaf path (1 -> 2 -> 4, or 1 -> 2 -> 5) has 2 edges.
     */
    public int height(TreeNode root) {
        // TODO: Implement your solution here.
        return 0;
    }
}
