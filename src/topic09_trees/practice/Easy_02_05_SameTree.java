package topic09_trees.practice;

import java.util.Deque;
import java.util.ArrayDeque;
import topic09_trees.TreeNode;

public class Easy_02_05_SameTree {

    /**
     * Drill 5/5 for Easy_02_SameTree — Iterative Same Tree
     * Technique: capstone — the same structural/value comparison performed by
     * Easy_02_SameTree, but the recursive call stack is replaced by an explicit Deque of
     * node PAIRS: push (p, q) pairs, pop and compare, and push their children pairs.
     *
     * Given the roots of two binary trees p and q, return true if they are structurally
     * identical with equal node values, implemented ITERATIVELY (no recursion) using a
     * stack or queue of node pairs.
     *
     * Example:
     * Input: p = [1,2,3], q = [1,2,3]
     * Output: true
     */
    public boolean isSameTreeIterative(TreeNode p, TreeNode q) {
        // TODO: Implement your solution here.
        return false;
    }
}
