package topic09_trees.practice;

import java.util.Queue;
import java.util.LinkedList;
import topic09_trees.TreeNode;

public class Easy_05_05_InvertTree {

    /**
     * Drill 5/5 for Easy_05_InvertTree — Iterative Invert (BFS)
     * Technique: capstone — the same "swap left/right at every node" operation performed
     * by Easy_05_InvertTree, but driven by an explicit BFS queue instead of recursion:
     * dequeue a node, swap its children, enqueue both children.
     *
     * Given the root of a binary tree, invert it (mirror every node's children) in place,
     * implemented ITERATIVELY (no recursion) using a queue, and return the modified root.
     *
     * Example:
     * Input: root = [4,2,7,1,3,6,9]
     * Output: [4,7,2,9,6,3,1]
     */
    public TreeNode invertTreeIterative(TreeNode root) {
        // TODO: Implement your solution here.
        return null;
    }
}
