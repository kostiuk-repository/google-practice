package topic09_trees.practice;

import topic09_trees.TreeNode;

public class Easy_04_01_SubtreeOfAnother {

    /**
     * Drill 1/5 for Easy_04_SubtreeOfAnother — Contains Value
     * Technique: Easy_04_SubtreeOfAnother visits every node of root and, at each one,
     * runs a full matching check. This drill isolates just the "visit every node of root"
     * search half, without the isSameTree matching helper — the condition checked at each
     * node is a simple value equality instead.
     *
     * Given the root of a binary tree and an integer target, return true if ANY node in
     * root holds the value target.
     *
     * Example:
     * Input: root = [3,4,5,1,2], target = 1
     * Output: true
     */
    public boolean containsValue(TreeNode root, int target) {
        // TODO: Implement your solution here.
        return false;
    }
}
