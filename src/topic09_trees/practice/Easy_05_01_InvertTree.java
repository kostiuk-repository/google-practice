package topic09_trees.practice;

import topic09_trees.TreeNode;

public class Easy_05_01_InvertTree {

    /**
     * Drill 1/5 for Easy_05_InvertTree — Swap Root Children Only
     * Technique: Easy_05_InvertTree swaps left/right children at EVERY node recursively.
     * This drill isolates the single-node swap step itself — no recursion — swapping only
     * root.left and root.right.
     *
     * Given the root of a binary tree, swap root's left and right children in place (do
     * NOT recurse into the subtrees) and return the modified root.
     *
     * Example:
     * Input: root = [1,2,3,4,5,6,7]
     * Output: [1,3,2,4,5,6,7]
     * Explanation: only the root's immediate children (2 and 3) are swapped; their own
     * children (4,5 and 6,7) stay attached to the same swapped parent unchanged.
     */
    public TreeNode swapRootChildren(TreeNode root) {
        // TODO: Implement your solution here.
        return null;
    }
}
