package topic09_trees.practice;

import topic09_trees.TreeNode;

public class Easy_03_01_SymmetricTree {

    /**
     * Drill 1/5 for Easy_03_SymmetricTree — Two-Tree Mirror Helper
     * Technique: Easy_03_SymmetricTree checks symmetry by internally comparing root.left
     * against root.right with a mirror helper. This drill isolates that helper as its own
     * top-level method comparing two INDEPENDENT trees for mirror-equality.
     *
     * Given the roots of two separate binary trees t1 and t2, return true if t2 is the
     * exact mirror image of t1 (t1.val == t2.val, and t1's left subtree mirrors t2's
     * right subtree, while t1's right subtree mirrors t2's left subtree).
     *
     * Example:
     * Input: t1 = [1,2,3], t2 = [1,3,2]
     * Output: true
     */
    public boolean isMirror(TreeNode t1, TreeNode t2) {
        // TODO: Implement your solution here.
        return false;
    }
}
