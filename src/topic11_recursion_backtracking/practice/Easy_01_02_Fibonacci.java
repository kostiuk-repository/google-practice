package topic11_recursion_backtracking.practice;

public class Easy_01_02_Fibonacci {

    /**
     * Drill 2/5 for Easy_01_Fibonacci — Fibonacci-Like Sequence With Custom Seeds
     * Technique: same two-term recurrence g(n) = g(n-1) + g(n-2), but the two starting
     * values are generalized into parameters instead of being hardcoded as 0 and 1.
     *
     * A sequence is defined by g(0) = a, g(1) = b, and g(k) = g(k-1) + g(k-2) for k >= 2.
     * Given the seeds a, b, and an index n, return g(n) using recursion (with memoization).
     *
     * Example:
     * Input: a = 3, b = 5, n = 4
     * Output: 21
     * Explanation: g = [3,5,8,13,21], so g(4) = 21.
     */
    public int fibWithSeeds(int a, int b, int n) {
        // TODO: Implement your solution here.
        return 0;
    }
}
