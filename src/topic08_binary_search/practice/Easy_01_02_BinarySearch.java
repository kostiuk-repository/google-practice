package topic08_binary_search.practice;

public class Easy_01_02_BinarySearch {

    /**
     * Drill 2/5 for Easy_01_BinarySearch — Search a Descending Array
     * Technique: the same lo/hi narrowing as Easy_01_BinarySearch, but the array is sorted
     * in DESCENDING order, so the branch that decides which half to keep must be flipped —
     * a mirror image of the ascending comparison.
     *
     * Given an array of distinct integers sorted in descending order and a target value,
     * return the index of target using binary search, or -1 if it is not present.
     *
     * Example:
     * Input: nums = [12,9,5,3,0,-4], target = 5
     * Output: 2
     */
    public int searchDescending(int[] nums, int target) {
        // TODO: Implement your solution here.
        return 0;
    }
}
