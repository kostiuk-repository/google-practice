package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_08_05_IntersectionOfTwoLists {

    /**
     * Drill 5/5 for Easy_08_IntersectionOfTwoLists — Common Intersection Of Three Lists
     * Technique: capstone — generalizes Easy_08_IntersectionOfTwoLists's pairwise switch-heads
     * trick to three lists, by finding the pairwise intersection of the first two, then finding
     * the pairwise intersection of that result with the third list.
     *
     * Given the heads of three singly linked lists headA, headB, and headC, return the node at
     * which all three lists intersect (share a common tail), or null if there is no node common
     * to all three.
     *
     * Example:
     * Input: headA = [4,1,8,4,5], headB = [5,6,1,8,4,5], headC = [9,8,4,5]
     * Output: the node with value 8
     */
    public ListNode commonIntersectionOfThree(ListNode headA, ListNode headB, ListNode headC) {
        // TODO: Implement your solution here.
        return null;
    }
}
