package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_04_05_MiddleOfLinkedList {

    /**
     * Drill 5/5 for Easy_04_MiddleOfLinkedList — Split Into Two Halves
     * Technique: capstone — reuses the exact slow/fast (1x/2x) walk from
     * Easy_04_MiddleOfLinkedList to locate the middle, then extends it by cutting the list's
     * next pointer there to produce two independent sublists.
     *
     * Given the head of a singly linked list, split it into two halves and return both heads as a
     * two-element array [firstHalfHead, secondHalfHead]. If the list has an odd number of nodes,
     * the extra node goes to the FIRST half.
     *
     * Example:
     * Input: head = [1,2,3,4,5]
     * Output: [[1,2,3], [4,5]]
     */
    public ListNode[] splitInHalf(ListNode head) {
        // TODO: Implement your solution here.
        return new ListNode[0];
    }
}
