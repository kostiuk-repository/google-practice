package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_03_01_LinkedListCycle {

    /**
     * Drill 1/5 for Easy_03_LinkedListCycle — Find The Cycle Start Node
     * Technique: Floyd's Tortoise and Hare, phase 2 — after slow/fast meet inside the cycle
     * (same phase 1 as Easy_03_LinkedListCycle), reset one pointer to head and advance both
     * one step at a time; they meet again exactly at the cycle's entry node.
     *
     * Given the head of a linked list, return the node where the cycle begins. If there is no
     * cycle, return null. Solve it in O(1) auxiliary space.
     *
     * Example:
     * Input: head = [3,2,0,-4] where the tail connects back to node with value 2
     * Output: the node with value 2
     */
    public ListNode findCycleStart(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
