package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_01_01_RemoveElements {

    /**
     * Drill 1/5 for Easy_01_RemoveElements — Delete Node At Position
     * Technique: dummy sentinel + prev/curr traversal, same shape as Easy_01_RemoveElements but
     * the removal condition is a 0-indexed POSITION instead of a value match.
     *
     * Given the head of a linked list and a 0-indexed integer position, remove the node at that
     * position and return the new head. You may assume position is a valid index within the list.
     *
     * Example:
     * Input: head = [1,2,3,4,5], position = 2
     * Output: [1,2,4,5]
     */
    public ListNode deleteAtPosition(ListNode head, int position) {
        // TODO: Implement your solution here.
        return null;
    }
}
