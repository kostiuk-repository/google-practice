package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_09_04_BinaryToInteger {

    /**
     * Drill 4/5 for Easy_09_BinaryToInteger — Bitwise XOR Of Two Binary Lists
     * Technique: combines two lists into the same left-to-right accumulate loop used in
     * Easy_09_BinaryToInteger — instead of accumulating one list's bits into a value, walk two
     * equal-length binary lists in lockstep and accumulate the XOR of their bits at each position.
     *
     * Given the heads of two singly-linked lists a and b of equal length, whose node values are
     * each 0 or 1 (most significant bit at head), return the decimal value of the bitwise XOR of
     * the two binary numbers they represent.
     *
     * Example:
     * Input: a = [1,0,1], b = [0,1,1]
     * Output: 6
     * Explanation: 101 XOR 011 = 110 = 6.
     */
    public int bitwiseXor(ListNode a, ListNode b) {
        // TODO: Implement your solution here.
        return 0;
    }
}
