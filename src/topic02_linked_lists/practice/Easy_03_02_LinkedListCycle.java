package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_03_02_LinkedListCycle {

    /**
     * Drill 2/5 for Easy_03_LinkedListCycle — Cycle Length
     * Technique: Floyd's Tortoise and Hare to find a meeting point (same as
     * Easy_03_LinkedListCycle), then keep one pointer fixed and walk the other around the loop
     * counting steps until it returns to the meeting point — that count is the cycle's length.
     *
     * Given the head of a linked list, return the number of nodes contained in its cycle. If
     * there is no cycle, return 0. Solve it in O(1) auxiliary space.
     *
     * Example:
     * Input: head = [3,2,0,-4] where the tail connects back to node with value 2 (cycle has 3 nodes)
     * Output: 3
     */
    public int cycleLength(ListNode head) {
        // TODO: Implement your solution here.
        return 0;
    }
}
