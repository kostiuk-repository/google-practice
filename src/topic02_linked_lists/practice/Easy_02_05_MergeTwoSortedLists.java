package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_02_05_MergeTwoSortedLists {

    /**
     * Drill 5/5 for Easy_02_MergeTwoSortedLists — Merge By Absolute Value
     * Technique: capstone — same two-pointer merge with dummy head as Easy_02_MergeTwoSortedLists,
     * but generalizes the comparison KEY: nodes are compared by Math.abs(val) instead of val
     * directly, while the original signed values are preserved in the output.
     *
     * You are given the heads of two linked lists list1 and list2, each sorted in ascending order
     * of the ABSOLUTE VALUE of their node values (e.g. [-1, 2, -3, 4]). Merge them into one list,
     * still sorted by ascending absolute value, by splicing existing nodes. Return the head.
     *
     * Example:
     * Input: list1 = [1,-2,3], list2 = [-1,2,4]
     * Output: [1,-1,-2,2,3,4]
     */
    public ListNode mergeByAbsoluteValue(ListNode list1, ListNode list2) {
        // TODO: Implement your solution here.
        return null;
    }
}
