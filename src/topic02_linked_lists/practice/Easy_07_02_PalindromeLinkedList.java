package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_07_02_PalindromeLinkedList {

    /**
     * Drill 2/5 for Easy_07_PalindromeLinkedList — Compare Two Given Halves
     * Technique: isolates the "compare" step of Easy_07_PalindromeLinkedList's three-step
     * strategy — given the two halves ALREADY split and the second one already reversed, only
     * the value-by-value comparison remains.
     *
     * Given the heads of two singly linked lists firstHalf and secondHalf that are guaranteed to
     * be the same length, return true if secondHalf, when walked from its head, is the exact
     * reverse of firstHalf's value sequence — i.e. firstHalf.val == secondHalf's LAST value,
     * firstHalf.next.val == secondHalf's SECOND-TO-LAST value, and so on.
     *
     * Example:
     * Input: firstHalf = [1,2], secondHalf = [2,1]
     * Output: true
     */
    public boolean isSecondHalfReverseOfFirstHalf(ListNode firstHalf, ListNode secondHalf) {
        // TODO: Implement your solution here.
        return false;
    }
}
