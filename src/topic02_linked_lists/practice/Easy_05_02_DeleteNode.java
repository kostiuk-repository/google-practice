package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_05_02_DeleteNode {

    /**
     * Drill 2/5 for Easy_05_DeleteNode — Delete Two Nodes
     * Technique: generalizes the count parameter of Easy_05_DeleteNode's copy-trick — apply the
     * "copy next's value forward, then skip" operation twice in a row to remove two nodes using
     * only a reference to the first one.
     *
     * Write a function to delete a node AND the node immediately after it in a singly linked
     * list, given only a reference to the first of the two nodes to be deleted (not the head).
     * It is guaranteed that node has at least two nodes after it in the list.
     *
     * Example:
     * Input: node = Node(5) (from list 4 -> 5 -> 1 -> 9 -> 7)
     * Output: List becomes 4 -> 9 -> 7 (nodes 5 and 1 are deleted)
     */
    public void deleteTwoNodes(ListNode node) {
        // TODO: Implement your solution here.
    }
}
