package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_08_04_IntersectionOfTwoLists {

    /**
     * Drill 4/5 for Easy_08_IntersectionOfTwoLists — Classify The Relationship
     * Technique: the same switch-heads two-pointer walk from Easy_08_IntersectionOfTwoLists,
     * generalized to distinguish two different kinds of "intersection" instead of a single
     * yes/no outcome.
     *
     * Given the heads of two singly linked lists headA and headB, return 0 if they do not
     * intersect at all, 1 if headA and headB are literally the same starting node (the two lists
     * are entirely identical), or 2 if they intersect partially (share a common tail but have
     * different starting nodes). Solve it in O(N + M) time and O(1) space.
     *
     * Example:
     * Input: headA = [4,1,8,4,5], headB = [5,6,1,8,4,5]
     * Output: 2
     */
    public int intersectionStatus(ListNode headA, ListNode headB) {
        // TODO: Implement your solution here.
        return 0;
    }
}
