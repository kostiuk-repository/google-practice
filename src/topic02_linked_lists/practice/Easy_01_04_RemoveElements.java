package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_01_04_RemoveElements {

    /**
     * Drill 4/5 for Easy_01_RemoveElements — Remove All Occurrences And Count Them
     * Technique: dummy sentinel + prev/curr traversal identical to Easy_01_RemoveElements,
     * combined with a running counter of how many nodes were spliced out.
     *
     * Given the head of a linked list, an integer val, and a 1-element output array countOut,
     * remove every node whose value equals val, store the number of removed nodes in
     * countOut[0], and return the new head.
     *
     * Example:
     * Input: head = [6,1,2,6,3,6], val = 6, countOut = new int[1]
     * Output: head = [1,2,3], countOut[0] = 3
     */
    public ListNode removeAllAndCount(ListNode head, int val, int[] countOut) {
        // TODO: Implement your solution here.
        return null;
    }
}
