package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_02_03_MergeTwoSortedLists {

    /**
     * Drill 3/5 for Easy_02_MergeTwoSortedLists — Merge Without Duplicates
     * Technique: the same two-pointer comparison + dummy-head splicing as
     * Easy_02_MergeTwoSortedLists, combined with a second concept — skip a node if its value
     * equals the value most recently appended to the result.
     *
     * You are given the heads of two sorted (ascending) linked lists list1 and list2. Merge them
     * into one sorted list by splicing existing nodes, but skip any node whose value equals the
     * value of the last node already placed in the result, so the output contains no duplicate
     * values. Return the head.
     *
     * Example:
     * Input: list1 = [1,2,4], list2 = [1,3,4]
     * Output: [1,2,3,4]
     */
    public ListNode mergeUnique(ListNode list1, ListNode list2) {
        // TODO: Implement your solution here.
        return null;
    }
}
