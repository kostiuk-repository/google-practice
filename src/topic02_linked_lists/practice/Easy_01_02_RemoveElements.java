package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_01_02_RemoveElements {

    /**
     * Drill 2/5 for Easy_01_RemoveElements — Remove First Occurrence Only
     * Technique: dummy sentinel + prev/curr traversal, but stop after the first match instead of
     * scanning the whole list (early-exit variant of Easy_01_RemoveElements).
     *
     * Given the head of a linked list and an integer val, remove only the FIRST node whose value
     * equals val (leave any later occurrences of that value untouched) and return the new head.
     *
     * Example:
     * Input: head = [1,2,6,3,6,4], val = 6
     * Output: [1,2,3,6,4]
     */
    public ListNode removeFirstOccurrence(ListNode head, int val) {
        // TODO: Implement your solution here.
        return null;
    }
}
