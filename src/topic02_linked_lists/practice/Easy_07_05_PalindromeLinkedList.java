package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_07_05_PalindromeLinkedList {

    /**
     * Drill 5/5 for Easy_07_PalindromeLinkedList — Check And Restore Original Order
     * Technique: capstone — performs the full three-step strategy from
     * Easy_07_PalindromeLinkedList (find middle, reverse second half, compare), then adds a
     * fourth step: reverse the second half back again so the list is left EXACTLY as it was
     * passed in, with no observable side effects.
     *
     * Given the head of a singly linked list, determine whether it is a palindrome using O(N)
     * time and O(1) extra space, and ensure that after the method returns, the list's node order
     * is identical to what it was before the call (i.e. the temporary reversal used internally
     * must be undone).
     *
     * Example:
     * Input: head = [1,2,2,1]
     * Output: true (and head still traverses as 1 -> 2 -> 2 -> 1 afterward)
     */
    public boolean isPalindromeRestoring(ListNode head) {
        // TODO: Implement your solution here.
        return false;
    }
}
