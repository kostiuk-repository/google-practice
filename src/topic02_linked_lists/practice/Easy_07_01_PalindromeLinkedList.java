package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_07_01_PalindromeLinkedList {

    /**
     * Drill 1/5 for Easy_07_PalindromeLinkedList — Reverse A Linked List
     * Technique: isolates the "reverse" step of the three-step strategy (find middle -> reverse
     * second half -> compare) used in Easy_07_PalindromeLinkedList — this is the building block
     * needed before the second half can be compared to the first.
     *
     * Given the head of a singly linked list, reverse it in place and return the new head. Do
     * not allocate new nodes; only relink existing ones.
     *
     * Example:
     * Input: head = [1,2,3,4]
     * Output: [4,3,2,1]
     */
    public ListNode reverseList(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
