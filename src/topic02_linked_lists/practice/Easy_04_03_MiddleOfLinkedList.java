package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_04_03_MiddleOfLinkedList {

    /**
     * Drill 3/5 for Easy_04_MiddleOfLinkedList — Delete The Middle Node
     * Technique: the same slow/fast (1x/2x) walk from Easy_04_MiddleOfLinkedList, combined with a
     * second concept — track the node just BEFORE slow (a trailing "prev" pointer) so the middle
     * node can be unlinked once found.
     *
     * Given the head of a singly linked list, delete the middle node (using the "second middle"
     * convention for even-length lists, same as Easy_04_MiddleOfLinkedList) and return the head
     * of the resulting list. If the list has only one node, return null.
     *
     * Example:
     * Input: head = [1,2,3,4,5]
     * Output: [1,2,4,5]
     */
    public ListNode deleteMiddleNode(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
