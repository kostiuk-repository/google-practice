package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_06_02_RemoveDuplicates {

    /**
     * Drill 2/5 for Easy_06_RemoveDuplicates — Remove ALL Duplicate Groups
     * Technique: a stricter variant of the curr/curr.next comparison from
     * Easy_06_RemoveDuplicates — instead of keeping one copy of each duplicated value, remove the
     * ENTIRE run of duplicates. Because the head itself may need removal, this requires a dummy
     * sentinel + prev pointer rather than a single curr pointer.
     *
     * Given the head of a sorted linked list, delete all nodes that have duplicate values,
     * leaving only distinct numbers from the original list (nodes that appeared exactly once).
     * Return the new head.
     *
     * Example:
     * Input: head = [1,2,3,3,4,4,5]
     * Output: [1,2,5]
     */
    public ListNode removeAllDuplicateGroups(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
