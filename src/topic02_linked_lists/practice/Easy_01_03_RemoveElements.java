package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_01_03_RemoveElements {

    /**
     * Drill 3/5 for Easy_01_RemoveElements — Remove Values Outside a Range
     * Technique: dummy sentinel + prev/curr traversal, generalizing the single-value predicate of
     * Easy_01_RemoveElements to a range check.
     *
     * Given the head of a linked list and two integers low and high, remove every node whose
     * value is NOT within the inclusive range [low, high], and return the new head.
     *
     * Example:
     * Input: head = [1,5,2,9,3,7], low = 2, high = 7
     * Output: [5,2,3,7]
     */
    public ListNode removeOutOfRange(ListNode head, int low, int high) {
        // TODO: Implement your solution here.
        return null;
    }
}
