package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_06_03_RemoveDuplicates {

    /**
     * Drill 3/5 for Easy_06_RemoveDuplicates — Collapse Adjacent Duplicates In An Unsorted List
     * Technique: same curr/curr.next comparison from Easy_06_RemoveDuplicates, but applied to a
     * list that is NOT guaranteed to be sorted — only literally adjacent equal values collapse,
     * non-adjacent repeats of the same value are left alone.
     *
     * Given the head of a singly linked list that may be unsorted, remove a node whenever its
     * value equals the value of the immediately preceding node (adjacent duplicates only, no
     * assumption that equal values are grouped together). Return the new head.
     *
     * Example:
     * Input: head = [1,1,2,3,3,2,2]
     * Output: [1,2,3,2]
     */
    public ListNode removeAdjacentDuplicatesUnsorted(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
