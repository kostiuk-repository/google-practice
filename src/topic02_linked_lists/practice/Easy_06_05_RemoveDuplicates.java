package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_06_05_RemoveDuplicates {

    /**
     * Drill 5/5 for Easy_06_RemoveDuplicates — Collapse Runs Into Their Counts
     * Technique: capstone — same curr/curr.next scan used to detect duplicate runs in
     * Easy_06_RemoveDuplicates, extended to replace each run with a single node holding the
     * number of times that value occurred instead of just keeping one copy of the value.
     *
     * Given the head of a sorted linked list, collapse every run of consecutive equal values into
     * a single node whose value is the COUNT of how many times that value occurred. Return the
     * new head.
     *
     * Example:
     * Input: head = [1,1,2,3,3,3]
     * Output: [2,1,3]
     * Explanation: value 1 occurs 2 times, value 2 occurs 1 time, value 3 occurs 3 times.
     */
    public ListNode collapseWithCounts(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
