package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_02_02_MergeTwoSortedLists {

    /**
     * Drill 2/5 for Easy_02_MergeTwoSortedLists — Zip Alternate
     * Technique: dummy head + tail pointer splicing exactly like Easy_02_MergeTwoSortedLists, but
     * isolated from the comparison step — nodes are spliced in strict alternation instead of by value.
     *
     * Given the heads of two linked lists list1 and list2 (not necessarily sorted, and possibly of
     * different lengths), splice their nodes together by alternating one node from list1, one node
     * from list2, one from list1, and so on. When one list runs out, append the remaining tail of
     * the other list as-is. Return the head of the resulting list.
     *
     * Example:
     * Input: list1 = [1,3,5], list2 = [2,4,6,8,10]
     * Output: [1,2,3,4,5,6,8,10]
     */
    public ListNode zipAlternate(ListNode list1, ListNode list2) {
        // TODO: Implement your solution here.
        return null;
    }
}
