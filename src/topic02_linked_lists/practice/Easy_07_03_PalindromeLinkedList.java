package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_07_03_PalindromeLinkedList {

    /**
     * Drill 3/5 for Easy_07_PalindromeLinkedList — Odd-Length Palindrome, Skip The Middle
     * Technique: the full three-step strategy from Easy_07_PalindromeLinkedList (find middle via
     * fast/slow, reverse second half, compare), but focused on the ODD-length edge case where the
     * single true middle element must be excluded from the comparison entirely.
     *
     * Given the head of a singly linked list that is guaranteed to have an ODD number of nodes,
     * return true if it is a palindrome, correctly skipping the exact middle node (which never
     * needs to match anything) during the comparison. Solve it in O(N) time and O(1) space.
     *
     * Example:
     * Input: head = [1,2,3,2,1]
     * Output: true
     */
    public boolean isPalindromeOddSkipMiddle(ListNode head) {
        // TODO: Implement your solution here.
        return false;
    }
}
