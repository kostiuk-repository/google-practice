package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_02_01_MergeTwoSortedLists {

    /**
     * Drill 1/5 for Easy_02_MergeTwoSortedLists — Merge Into Descending Order
     * Technique: two-pointer merge with dummy head and node splicing, same shape as
     * Easy_02_MergeTwoSortedLists but the comparator is flipped (mirror direction).
     *
     * You are given the heads of two linked lists, list1 and list2, each sorted in DESCENDING
     * order. Merge them into a single list still sorted in descending order by splicing together
     * the existing nodes (do not create new nodes), and return the head.
     *
     * Example:
     * Input: list1 = [4,2,1], list2 = [4,3,1]
     * Output: [4,4,3,2,1,1]
     */
    public ListNode mergeDescending(ListNode list1, ListNode list2) {
        // TODO: Implement your solution here.
        return null;
    }
}
