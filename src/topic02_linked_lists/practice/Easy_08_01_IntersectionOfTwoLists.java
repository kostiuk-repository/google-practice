package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_08_01_IntersectionOfTwoLists {

    /**
     * Drill 1/5 for Easy_08_IntersectionOfTwoLists — Align By Length Difference
     * Technique: an alternate way to reach the same O(N+M) time, O(1) space result as
     * Easy_08_IntersectionOfTwoLists's switch-heads two-pointer trick — first measure both
     * lengths, then advance the pointer on the longer list by the difference before walking both
     * together.
     *
     * Given the heads of two singly linked lists headA and headB, return the node at which they
     * intersect, or null if they do not intersect. Solve it by first computing each list's
     * length, advancing the pointer on the longer list by the length difference, then walking
     * both pointers one step at a time until they meet.
     *
     * Example:
     * Input: headA = [4,1,8,4,5], headB = [5,6,1,8,4,5] (share the node with value 8 onward)
     * Output: the node with value 8
     */
    public ListNode getIntersectionNodeByLengthAlign(ListNode headA, ListNode headB) {
        // TODO: Implement your solution here.
        return null;
    }
}
