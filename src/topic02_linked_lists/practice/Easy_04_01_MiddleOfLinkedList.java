package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_04_01_MiddleOfLinkedList {

    /**
     * Drill 1/5 for Easy_04_MiddleOfLinkedList — Return The First Middle Node
     * Technique: same slow/fast (1x/2x) pointer walk as Easy_04_MiddleOfLinkedList, but the fast
     * pointer's loop condition is tweaked so that for even-length lists, slow lands on the FIRST
     * of the two middle nodes instead of the second.
     *
     * Given the head of a singly linked list, return the middle node. If there are two middle
     * nodes (even length), return the FIRST middle node.
     *
     * Example:
     * Input: head = [1,2,3,4,5,6]
     * Output: Node(3) (list from middle is [3,4,5,6])
     */
    public ListNode firstMiddleNode(ListNode head) {
        // TODO: Implement your solution here.
        return null;
    }
}
