package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_04_04_MiddleOfLinkedList {

    /**
     * Drill 4/5 for Easy_04_MiddleOfLinkedList — Verify A Candidate Is The Middle
     * Technique: the same slow/fast pointer walk as Easy_04_MiddleOfLinkedList, but used to VERIFY
     * a given node reference rather than to return a newly found one.
     *
     * Given the head of a singly linked list and a reference to some node candidate that is known
     * to be part of the list, return true if candidate is the middle node of the list (using the
     * "second middle" convention for even-length lists), false otherwise.
     *
     * Example:
     * Input: head = [1,2,3,4,5], candidate = Node(3)
     * Output: true
     */
    public boolean isMiddleNode(ListNode head, ListNode candidate) {
        // TODO: Implement your solution here.
        return false;
    }
}
