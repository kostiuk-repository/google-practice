package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_02_04_MergeTwoSortedLists {

    /**
     * Drill 4/5 for Easy_02_MergeTwoSortedLists — Verify A Merge
     * Technique: same two-pointer walk as Easy_02_MergeTwoSortedLists, but used to VERIFY a
     * candidate result instead of CONSTRUCT one — a genuinely different direction of the technique.
     *
     * Given two sorted (ascending) linked lists list1 and list2, and a third list merged, determine
     * whether merged is a valid stable merge of list1 and list2 — that is, merged contains exactly
     * the values of list1 and list2 combined, still in ascending order, with list1's and list2's
     * relative internal orders preserved. Return true if valid, false otherwise.
     *
     * Example:
     * Input: list1 = [1,3,5], list2 = [2,4], merged = [1,2,3,4,5]
     * Output: true
     */
    public boolean isValidMerge(ListNode list1, ListNode list2, ListNode merged) {
        // TODO: Implement your solution here.
        return false;
    }
}
