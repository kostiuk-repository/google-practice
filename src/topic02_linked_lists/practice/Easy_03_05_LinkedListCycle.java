package topic02_linked_lists.practice;

import topic02_linked_lists.ListNode;

public class Easy_03_05_LinkedListCycle {

    /**
     * Drill 5/5 for Easy_03_LinkedListCycle — Cycle Length Parity
     * Technique: capstone — combines cycle detection, cycle-start finding, and cycle-length
     * counting (drills 1-2 of this set, built on Easy_03_LinkedListCycle's Floyd's algorithm)
     * into a single function that also classifies the length's parity.
     *
     * Given the head of a linked list, return -1 if there is no cycle. If there is a cycle,
     * return 0 if the cycle's length is even, or 1 if the cycle's length is odd. Solve it in
     * O(1) auxiliary space.
     *
     * Example:
     * Input: head = [1,2,3,4] where the tail connects back to node with value 2 (cycle length 3)
     * Output: 1
     */
    public int cycleLengthParity(ListNode head) {
        // TODO: Implement your solution here.
        return 0;
    }
}
